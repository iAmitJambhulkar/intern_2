import 'package:flutter/material.dart';

class SavedContent extends StatelessWidget {
  const SavedContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Saved Content', style: TextStyle(fontSize: 16)),
    );
  }
}
