import 'package:flutter/material.dart';

class SharedContent extends StatelessWidget {
  const SharedContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Shared Content', style: TextStyle(fontSize: 16)),
    );
  }
}
